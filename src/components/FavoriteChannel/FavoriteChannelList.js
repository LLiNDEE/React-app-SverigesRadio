import React,{useContext} from 'react';
import Context from '../Context';


export default function FavoriteChannelList(){

    const {favoriteChannels, setFavoriteChannels} = useContext(Context);

    return(
        <div>
            
        </div>
    );
}